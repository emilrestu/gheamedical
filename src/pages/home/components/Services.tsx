import { Col, Row } from 'antd';
import React from 'react';

const Services: React.FC = () => {
    return (
        <Row className="home-content-wrapper" gutter={[16, 16]}>
            <Col>services</Col>
        </Row>
    );
};

export default Services;
